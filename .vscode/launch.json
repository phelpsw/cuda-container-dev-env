{
    "version": "2.0.0",
    "configurations": [
        {
            "name": "CUDA C++ Debug",
            "type": "cuda-gdb",
            "request": "launch",
            "cwd": "${workspaceFolder}",
            "program": "${workspaceFolder}/main"
        }
    ]
}